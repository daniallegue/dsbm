# Core: config plumbing + numerics, needed by every entrypoint (main.py, test.py,
# run_diagnostic.py, DSBM-Gaussian.py)
hydra-core==1.3.5
omegaconf==2.3.1
# Bumped from 1.26.4 to satisfy mdtraj (tetrapeptide TPS experiment, below), which
# requires numpy 2.x; verified openmm/rdkit still import fine under 2.4.6 (noisy but
# harmless "compiled for NumPy 1.x" warnings from rdkit's legacy compat shim).
numpy>=2.0,<2.5
scipy==1.17.1
torch==2.13.0

# Sci-Plex3 rank diagnostic (run_diagnostic.py / bridge/diagnostics)
h5py==3.15.1
POT==0.9.7.post1
matplotlib==3.10.3

# Full image-transfer training/eval pipeline (main.py -> run_dbdsb.py / run_dsb.py /
# run_rf.py, test.py, DSBM-Gaussian.py). Not required just to run run_diagnostic.py.
torchvision==0.28.0
torchdiffeq==0.2.5
torchmetrics==1.9.0
pandas==2.3.3
seaborn==0.13.2
tqdm==4.66.2
Pillow==10.1.0
pytorch-lightning==2.6.5
wandb==0.28.0
accelerate>=1.14.0    # not exercised in this environment; pin if it causes issues
ninja>=1.13.0         # JIT-compiles the DDPM++ CUDA ops (bridge/models/ddpmpp/op)
lpips>=0.1.4          # backend for torchmetrics' LearnedPerceptualImagePatchSimilarity
torch-fidelity>=0.4.0 # backend for torchmetrics' FrechetInceptionDistance

# Optional: only needed for conf/launcher/slurm_*.yaml (cluster submission via Hydra)
# hydra-submitit-launcher

# Alanine dipeptide SO(3) bridge benchmark (bridge/data/alanine_dipeptide.py,
# conf/dataset/alanine_dipeptide.yaml): builds + simulates the capped dipeptide
# to generate the two basin marginals; not needed for any other dataset.
openmm==8.6.0
rdkit==2023.9.5

# Tetrapeptide TPS SE(3)^4 x T^28 R-IMF benchmark (bridge/data/tetrapeptide_tps.py,
# conf/dataset/tetrapeptide_tps.yaml, vendor/mdgen/): reads mdgen's prepped atom14
# .npy sims and converts to frames+torsions via vendored mdgen/geometry.py. mdtraj
# is also used by vendor/mdgen/scripts/prep_sims.py and by the sampling/export
# script to write out generated trajectories as .pdb/.xtc.
mdtraj>=1.11
biopython>=1.85   # vendor/mdgen/mdgen/protein.py (PDB writing)
dm-tree>=0.1.8    # vendor/mdgen/mdgen/residue_constants.py
# pyemma (mdgen/analysis.py's TICA/k-means/MSM fitting + the analyze_peptide_tps.py
# eval script) has no Windows wheels and fails to build from source against MSVC.
# It is NOT installed here -- use the separate `tps_eval` conda env instead
# (`conda create -n tps_eval -c conda-forge python=3.11 pyemma mdtraj pandas`).
# See vendor/mdgen/PROVENANCE.md.
